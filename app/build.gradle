apply plugin: 'java'

dependencies {
    compileOnly 'com.yuvimasory:orange-extensions:1.3.0'   // OSX support
    implementation project(':api')
    implementation project(':services')
}

version = parent.version
